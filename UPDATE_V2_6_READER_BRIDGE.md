# v2.6 - Reader Bridge

- NCAR search runs from the end-user browser against the official NCAR API (CORS).
- Exact NCAR search automatically retries a broader match.
- Render can enrich BOE/NCAR official URLs through Jina Reader when direct TCP/443 is blocked.
- The displayed legal source always remains the original government URL.
- Direct official-source retrieval remains enabled in parallel.
