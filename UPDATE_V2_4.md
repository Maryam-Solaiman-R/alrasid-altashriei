# تحديث 2.4 — Browser Fallback

- فصل مهلة اتصال NCAR من المتصفح عن مهلة طلب API لمنع إلغاء الطلب الرئيسي.
- ضبط عنوان Render الإنتاجي في `config.js`.
- جعل رابط Render الرئيسي يعرض واجهة الراصد مباشرة بدل JSON.
- إضافة مؤشر تشخيص في استجابة الوكيل يوضح عدد مستندات NCAR التي وصلت عبر Browser Fallback.
- الإبقاء على سياسة عدم عرض نتيجة تشريعية غير موثقة.

## بعد الرفع
انتظر اكتمال Deploy في Render، ثم افتح الرابط الرئيسي للخدمة واختبر البحث من واجهة الراصد، وليس من Swagger، لأن Browser Fallback يعمل من المتصفح.
